[pytest]
testpaths = tests
addopts = -q -m "not e2e and not network"
markers =
    e2e: 需要真实模型后端 (models.yaml 的 default), 服务不可用时自动 skip; 跑法: pytest -m e2e
    network: 需要外网 (wttr.in); 跑法: pytest -m network
